production:
  dialect: postgres
  datasource: host=${DB_HOST} dbname=${DB_NAME} user=${DB_USER} password=${DB_PASS} sslmode=disable
  dir: migrations
  table: migrations

local:
  dialect: postgres
  datasource: host=127.0.0.1 port=8001 dbname=d-api user=postgres password=postgresGOLANG sslmode=disable
  dir: migrations
  table: migrations
